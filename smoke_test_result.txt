# Smoke test result (25s run)

## Acceptance checks

1. **Dual start / fallback**
   - GL_DUAL START cam0=0 cam1=1
   - GL_DUAL PIPELINE_IDS, START_CALL, START_CALLED
   - cam1 ERROR code=2 (CAMERA_IN_USE) on this device → graceful single-stream

2. **seenStreams**
   - Requirement: "seenStreams contains cam0,cam1 (when dual)"
   - On this device: dual attempted, cam1 fails → seenStreams=[cam0] only. PASS (graceful fallback).

3. **GL_BLOB ~10 fps (not ~20)**
   - Observed: fps=9.8–10.0 throughout. PASS.

4. **No PROCESS ENDED / Fatal / FATAL EXCEPTION**
   - None in smoke_log.txt. PASS.

## Success log lines (excerpt)

GL_DISPATCH  thread=FrameDispatch started
GL_DUAL  DEV_MIXED_FACING START cam0=0 cam1=1
GL_DUAL  START cam0=0 cam1=1
GL_DUAL  PIPELINE_IDS cam0Id=0 cam1Id=1
GL_DUAL  START_CALL cam0=0 cam1=1
GL_DUAL  START_CALLED
GL_PIPE  cam0 OPENED cameraId=0
GL_PIPE  cam0 SESSION_OK targets=2 hasPreview=true
GL_PIPE  cam0 REPEAT_OK
GL_PIPE  cam1 ERROR cameraId=1 code=2
GL_PIPE  cam0 FIRST_FRAME cameraId=0 ...
GL_DUAL  IN_FIRST streamId=cam0 ...
GL_DUAL  PROCESS_PRIMARY_ONLY primary=cam0
GL_SYNC  seenStreams=[cam0], ...
GL_BLOB  fps=10.0 ...
(no FATAL EXCEPTION / PROCESS ENDED)

## Code change applied

MainActivity.kt: start cam0 first, then pipeline1?.start() after 400ms delay to reduce ERROR_CAMERA_IN_USE on some devices. On XT2527-3 (API 35) the second camera still returns code=2; app runs in single-stream mode with ~10 fps and no crash.
